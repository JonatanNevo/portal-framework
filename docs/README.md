# Documentation

## Building
make sure you have `uv` installed and available in your path.

then go to the project root and run
```shell
cmake --workflow build-docs
```

Then will generate and create the folder `docs/build/html` which can be opened in a browser or deployed into a static site host.